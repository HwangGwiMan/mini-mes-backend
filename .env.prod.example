# 운영 Docker용 환경 변수 예시 (같은 compose 파일에 --env-file .env.prod 로 사용)
# 복사: cp .env.prod.example .env.prod 후 값 수정
# 사용: docker compose -f docker-compose.db.yml --env-file .env.prod up -d
#       docker compose -f docker-compose.backend.yml --env-file .env.prod up -d
#       또는 통합: docker compose --env-file .env.prod up -d

POSTGRES_DB=mini_mes
POSTGRES_USER=postgres
POSTGRES_PASSWORD=반드시_강한_비밀번호_설정
POSTGRES_PORT=5432
BACKEND_PORT=8080
SPRING_PROFILES_ACTIVE=prod
RESTART_POLICY=unless-stopped
